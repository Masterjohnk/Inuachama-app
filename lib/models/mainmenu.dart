import 'package:flutter/cupertino.dart';

class MainMenu {
  IconData icon;
  String title, description;
  double value;
  int position;

  MainMenu(this.icon, this.title, this.description, this.value, this.position);
}
